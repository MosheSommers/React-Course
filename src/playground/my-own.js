const colors = ['red, green'];


const template = (

        <div>
            <p>{10 + 6 == 16 && 'I can do thisForget id'}</p>
        </div>
);


ReactDOM.render(template, document.getElementById('app'));